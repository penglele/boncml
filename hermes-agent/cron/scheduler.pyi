# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from cron.jobs import advance_next_run, get_due_jobs, mark_job_run, save_job_output
from hermes_cli.config import load_config
from hermes_constants import get_hermes_home
from hermes_time import now
from pathlib import Path
from typing import Any, Optional
import asyncio
import concurrent.futures
import fcntl
import json
import logging
import msvcrt
import os
import subprocess
import sys

fcntl = None
msvcrt = None
_KNOWN_DELIVERY_PLATFORMS = frozenset({'telegram', 'discord', 'slack', 'whatsapp', 'signal', 'matrix', 'mattermost', 'homeassistant', 'dingtalk', 'feishu', 'wecom', 'wecom_callback', 'weixin', 'sms', 'email', 'webhook', 'bluebubbles'})
SILENT_MARKER = '[SILENT]'
_LOCK_DIR = _hermes_home / 'cron'
_LOCK_FILE = _LOCK_DIR / '.tick.lock'
def _resolve_origin(job: dict) -> Optional[dict]:
    ...

def _resolve_delivery_target(job: dict) -> Optional[dict]:
    ...

_AUDIO_EXTS = frozenset({'.ogg', '.opus', '.mp3', '.wav', '.m4a'})
_VIDEO_EXTS = frozenset({'.mp4', '.mov', '.avi', '.mkv', '.webm', '.3gp'})
_IMAGE_EXTS = frozenset({'.jpg', '.jpeg', '.png', '.webp', '.gif'})
def _send_media_via_adapter(adapter: Any, chat_id: str, media_files: list, metadata: dict | None, loop: Any, job: dict) -> None:
    ...

def _deliver_result(job: dict, content: str, adapters: Any, loop: Any) -> Optional[str]:
    ...

_DEFAULT_SCRIPT_TIMEOUT = 120
_SCRIPT_TIMEOUT = _DEFAULT_SCRIPT_TIMEOUT
def _get_script_timeout() -> int:
    ...

def _run_job_script(script_path: str) -> tuple[bool, str]:
    ...

def _build_job_prompt(job: dict) -> str:
    ...

def run_job(job: dict) -> tuple[bool, str, str, Optional[str]]:
    ...

def tick(verbose: bool, adapters: Any, loop: Any) -> int:
    ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import asyncio
import concurrent
import concurrent.futures
import json
import logging
import os
import subprocess
import sys
import fcntl
import msvcrt
import pathlib
import typing
import hermes_constants
import hermes_cli
import hermes_cli.config
import hermes_time
import cron
import cron.jobs
import tools
import tools.send_message_tool
import gateway
import gateway.channel_directory
import gateway.config
import gateway.platforms
import gateway.platforms.base
import agent
import agent.redact
import tools.skills_tool
import run_agent
import hermes_state
import dotenv
import dotenv.load_dotenv
import yaml
import hermes_cli.runtime_provider
import agent.smart_model_routing
import agent.credential_pool