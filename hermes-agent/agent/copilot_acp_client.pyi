# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from __future__ import annotations
from collections import deque
from pathlib import Path
from types import SimpleNamespace
from typing import Any
from typing_extensions import Self
import json
import os
import queue
import re
import shlex
import subprocess
import threading
import time

ACP_MARKER_BASE_URL = 'acp://copilot'
_DEFAULT_TIMEOUT_SECONDS = 900.0
def _resolve_command() -> str:
    ...

def _resolve_args() -> list[str]:
    ...

def _jsonrpc_error(message_id: Any, code: int, message: str) -> dict[str, Any]:
    ...

def _format_messages_as_prompt(messages: list[dict[str, Any]], model: str | None, tools: list[dict[str, Any]] | None, tool_choice: Any) -> str:
    ...

def _render_message_content(content: Any) -> str:
    ...

def _extract_tool_calls_from_text(text: str) -> tuple[list[SimpleNamespace], str]:
    ...

def _ensure_path_within_cwd(path_text: str, cwd: str) -> Path:
    ...

class _ACPChatCompletions:
    def __init__(self: Self, client: 'CopilotACPClient') -> None: ...
    def create(self: Self) -> Any: ...

class _ACPChatNamespace:
    def __init__(self: Self, client: 'CopilotACPClient') -> None: ...

class CopilotACPClient:
    def __init__(self: Self) -> None: ...
    def close(self: Self) -> None: ...
    def _create_chat_completion(self: Self) -> Any: ...
    def _run_prompt(self: Self, prompt_text: str) -> tuple[str, str]: ...
    def _handle_server_message(self: Self, msg: dict[str, Any]) -> bool: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import __future__
import json
import os
import queue
import re
import shlex
import subprocess
import threading
import time
import collections
import collections.deque
import pathlib
import types
import typing