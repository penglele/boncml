# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from abc import ABC, abstractmethod
from typing import Any, Dict, List, Optional
from typing_extensions import Self
from dataclasses import dataclass

@dataclass
class ContextEngine(ABC):
    def name(self: Self) -> str: ...
    def update_from_response(self: Self, usage: Dict[str, Any]) -> None: ...
    def should_compress(self: Self, prompt_tokens: int) -> bool: ...
    def compress(self: Self, messages: List[Dict[str, Any]], current_tokens: int) -> List[Dict[str, Any]]: ...
    def should_compress_preflight(self: Self, messages: List[Dict[str, Any]]) -> bool: ...
    def on_session_start(self: Self, session_id: str) -> None: ...
    def on_session_end(self: Self, session_id: str, messages: List[Dict[str, Any]]) -> None: ...
    def on_session_reset(self: Self) -> None: ...
    def get_tool_schemas(self: Self) -> List[Dict[str, Any]]: ...
    def handle_tool_call(self: Self, name: str, args: Dict[str, Any]) -> str: ...
    def get_status(self: Self) -> Dict[str, Any]: ...
    def update_model(self: Self, model: str, context_length: int, base_url: str, api_key: str, provider: str) -> None: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import abc
import typing
import json