# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from agent.usage_pricing import CanonicalUsage, DEFAULT_PRICING, estimate_usage_cost, format_duration_compact, get_pricing, has_known_pricing
from collections import Counter, defaultdict
from datetime import datetime
from typing import Any, Dict, List
from typing_extensions import Self
import json
import time

_DEFAULT_PRICING = DEFAULT_PRICING
def _has_known_pricing(model_name: str, provider: str, base_url: str) -> bool:
    ...

def _estimate_cost(session_or_model: Dict[str, Any] | str, input_tokens: int, output_tokens: int) -> tuple[float, str]:
    ...

def _format_duration(seconds: float) -> str:
    ...

def _bar_chart(values: List[int], max_width: int) -> List[str]:
    ...

class InsightsEngine:
    def __init__(self: Self, db: Any) -> None: ...
    def generate(self: Self, days: int, source: str) -> Dict[str, Any]: ...
    def _get_sessions(self: Self, cutoff: float, source: str) -> List[Dict]: ...
    def _get_tool_usage(self: Self, cutoff: float, source: str) -> List[Dict]: ...
    def _get_message_stats(self: Self, cutoff: float, source: str) -> Dict: ...
    def _compute_overview(self: Self, sessions: List[Dict], message_stats: Dict) -> Dict: ...
    def _compute_model_breakdown(self: Self, sessions: List[Dict]) -> List[Dict]: ...
    def _compute_platform_breakdown(self: Self, sessions: List[Dict]) -> List[Dict]: ...
    def _compute_tool_breakdown(self: Self, tool_usage: List[Dict]) -> List[Dict]: ...
    def _compute_activity_patterns(self: Self, sessions: List[Dict]) -> Dict: ...
    def _compute_top_sessions(self: Self, sessions: List[Dict]) -> List[Dict]: ...
    def format_terminal(self: Self, report: Dict) -> str: ...
    def format_gateway(self: Self, report: Dict) -> str: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import json
import time
import collections
import collections.Counter
import collections.defaultdict
import datetime
import typing
import agent
import agent.usage_pricing