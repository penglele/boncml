# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from __future__ import annotations
from agent.memory_provider import MemoryProvider
from tools.registry import tool_error
from typing import Any, Dict, List, Optional
from typing_extensions import Self
import json
import logging
import re

def sanitize_context(text: str) -> str:
    ...

def build_memory_context_block(raw_context: str) -> str:
    ...

class MemoryManager:
    def __init__(self: Self) -> None: ...
    def add_provider(self: Self, provider: MemoryProvider) -> None: ...
    def providers(self: Self) -> List[MemoryProvider]: ...
    def get_provider(self: Self, name: str) -> Optional[MemoryProvider]: ...
    def build_system_prompt(self: Self) -> str: ...
    def prefetch_all(self: Self, query: str) -> str: ...
    def queue_prefetch_all(self: Self, query: str) -> None: ...
    def sync_all(self: Self, user_content: str, assistant_content: str) -> None: ...
    def get_all_tool_schemas(self: Self) -> List[Dict[str, Any]]: ...
    def get_all_tool_names(self: Self) -> set: ...
    def has_tool(self: Self, tool_name: str) -> bool: ...
    def handle_tool_call(self: Self, tool_name: str, args: Dict[str, Any]) -> str: ...
    def on_turn_start(self: Self, turn_number: int, message: str) -> None: ...
    def on_session_end(self: Self, messages: List[Dict[str, Any]]) -> None: ...
    def on_pre_compress(self: Self, messages: List[Dict[str, Any]]) -> str: ...
    def on_memory_write(self: Self, action: str, target: str, content: str) -> None: ...
    def on_delegation(self: Self, task: str, result: str) -> None: ...
    def shutdown_all(self: Self) -> None: ...
    def initialize_all(self: Self, session_id: str) -> None: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import __future__
import json
import logging
import re
import typing
import agent
import agent.memory_provider
import tools
import tools.registry
import hermes_constants