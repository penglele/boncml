# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from __future__ import annotations
from acp.schema import AgentCapabilities, AudioContentBlock, AuthMethod, AuthMethodAgent, AuthenticateResponse, AvailableCommand, AvailableCommandsUpdate, ClientCapabilities, EmbeddedResourceContentBlock, ForkSessionResponse, ImageContentBlock, Implementation, InitializeResponse, ListSessionsResponse, LoadSessionResponse, McpServerHttp, McpServerSse, McpServerStdio, NewSessionResponse, PromptResponse, ResourceContentBlock, ResumeSessionResponse, SessionCapabilities, SessionForkCapabilities, SessionInfo, SessionListCapabilities, SessionResumeCapabilities, SetSessionConfigOptionResponse, SetSessionModeResponse, SetSessionModelResponse, TextContentBlock, UnstructuredCommandInput, Usage
from acp_adapter.auth import detect_provider, has_provider
from acp_adapter.events import make_message_cb, make_step_cb, make_thinking_cb, make_tool_progress_cb
from acp_adapter.permissions import make_approval_callback
from acp_adapter.session import SessionManager, SessionState
from collections import defaultdict, deque
from concurrent.futures import ThreadPoolExecutor
from hermes_cli import __version__
from typing import Any, Deque, Optional
from typing_extensions import Self
import acp
import asyncio
import logging

HERMES_VERSION = '0.0.0'
def _extract_text(prompt: list[TextContentBlock | ImageContentBlock | AudioContentBlock | ResourceContentBlock | EmbeddedResourceContentBlock]) -> str:
    ...

class HermesACPAgent:
    def __init__(self: Self, session_manager: SessionManager | None) -> None: ...
    def on_connect(self: Self, conn: acp.Client) -> None: ...
    @classmethod
    def _available_commands(cls, ) -> list[AvailableCommand]: ...
    def _schedule_available_commands_update(self: Self, session_id: str) -> None: ...
    def _handle_slash_command(self: Self, text: str, state: SessionState) -> str | None: ...
    def _cmd_help(self: Self, args: str, state: SessionState) -> str: ...
    def _cmd_model(self: Self, args: str, state: SessionState) -> str: ...
    def _cmd_tools(self: Self, args: str, state: SessionState) -> str: ...
    def _cmd_context(self: Self, args: str, state: SessionState) -> str: ...
    def _cmd_reset(self: Self, args: str, state: SessionState) -> str: ...
    def _cmd_compact(self: Self, args: str, state: SessionState) -> str: ...
    def _cmd_version(self: Self, args: str, state: SessionState) -> str: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import __future__
import asyncio
import logging
import collections
import collections.defaultdict
import collections.deque
import concurrent
import concurrent.futures
import concurrent.futures.ThreadPoolExecutor
import typing
import acp
import acp.schema
import acp_adapter
import acp_adapter.auth
import acp_adapter.events
import acp_adapter.permissions
import acp_adapter.session
import hermes_cli
import hermes_cli.__version__
import tools
import tools.mcp_tool
import model_tools
import tools.terminal_tool
import hermes_cli.models
import agent
import agent.model_metadata