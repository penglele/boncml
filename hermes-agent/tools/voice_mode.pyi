# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from hermes_constants import is_termux
from typing import Any, Dict, List, Optional
from typing_extensions import Self
import logging
import os
import platform
import re
import shutil
import subprocess
import tempfile
import threading
import time
import wave

def _import_audio() -> Any:
    ...

def _audio_available() -> bool:
    ...

def _voice_capture_install_hint() -> str:
    ...

def _termux_microphone_command() -> Optional[str]:
    ...

def _termux_media_player_command() -> Optional[str]:
    ...

def _termux_api_app_installed() -> bool:
    ...

def _termux_voice_capture_available() -> bool:
    ...

def detect_audio_environment() -> dict:
    ...

SAMPLE_RATE = 16000
CHANNELS = 1
DTYPE = 'int16'
SAMPLE_WIDTH = 2
SILENCE_RMS_THRESHOLD = 200
SILENCE_DURATION_SECONDS = 3.0
def play_beep(frequency: int, duration: float, count: int) -> None:
    ...

class TermuxAudioRecorder:
    def __init__(self: Self) -> None: ...
    def is_recording(self: Self) -> bool: ...
    def elapsed_seconds(self: Self) -> float: ...
    def current_rms(self: Self) -> int: ...
    def start(self: Self, on_silence_stop: Any) -> None: ...
    def _stop_termux_recording(self: Self) -> None: ...
    def stop(self: Self) -> Optional[str]: ...
    def cancel(self: Self) -> None: ...
    def shutdown(self: Self) -> None: ...

class AudioRecorder:
    def __init__(self: Self) -> None: ...
    def elapsed_seconds(self: Self) -> float: ...
    def current_rms(self: Self) -> int: ...
    def _ensure_stream(self: Self) -> None: ...
    def start(self: Self, on_silence_stop: Any) -> None: ...
    def _close_stream_with_timeout(self: Self, timeout: float) -> None: ...
    def stop(self: Self) -> Optional[str]: ...
    def cancel(self: Self) -> None: ...
    def shutdown(self: Self) -> None: ...
    @staticmethod
    def _write_wav(audio_data: Any) -> str: ...

def create_audio_recorder() -> AudioRecorder | TermuxAudioRecorder:
    ...

WHISPER_HALLUCINATIONS = {'thank you.', 'thank you', 'thanks for watching.', 'thanks for watching', 'subscribe to my channel.', 'subscribe to my channel', 'like and subscribe.', 'like and subscribe', 'please subscribe.', 'please subscribe', 'thank you for watching.', 'thank you for watching', 'bye.', 'bye', 'you', 'the end.', 'the end', 'продолжение следует', 'продолжение следует...', 'sous-titres', "sous-titres réalisés par la communauté d'amara.org", 'sottotitoli creati dalla comunità amara.org', 'untertitel von stephanie geiges', 'amara.org', 'www.mooji.org', 'ご視聴ありがとうございました'}
def is_whisper_hallucination(transcript: str) -> bool:
    ...

def transcribe_recording(wav_path: str, model: Optional[str]) -> Dict[str, Any]:
    ...

_active_playback: Optional[subprocess.Popen] = None
def stop_playback() -> None:
    ...

def play_audio_file(file_path: str) -> bool:
    ...

def check_voice_requirements() -> Dict[str, Any]:
    ...

def cleanup_temp_recordings(max_age_seconds: int) -> int:
    ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import logging
import os
import platform
import re
import shutil
import subprocess
import tempfile
import threading
import time
import wave
import typing
import sounddevice
import numpy
import hermes_constants
import posixpath
import tools
import tools.transcription_tools