# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from datetime import datetime, timezone
from hermes_constants import get_hermes_home
from pathlib import Path, PurePosixPath
from tools.skills_guard import ScanResult, TRUSTED_REPOS, content_hash
from typing import Any, Dict, List, Optional, Tuple, Union
from typing_extensions import Self
from urllib.parse import urlparse, urlunparse
from dataclasses import dataclass
import hashlib
import httpx
import json
import logging
import os
import re
import shutil
import subprocess
import time
import yaml

SKILLS_DIR = HERMES_HOME / 'skills'
HUB_DIR = SKILLS_DIR / '.hub'
LOCK_FILE = HUB_DIR / 'lock.json'
QUARANTINE_DIR = HUB_DIR / 'quarantine'
AUDIT_LOG = HUB_DIR / 'audit.log'
TAPS_FILE = HUB_DIR / 'taps.json'
INDEX_CACHE_DIR = HUB_DIR / 'index-cache'
INDEX_CACHE_TTL = 3600
@dataclass
class SkillMeta:
@dataclass
class SkillBundle:
def _normalize_bundle_path(path_value: str) -> str:
    ...

def _validate_skill_name(name: str) -> str:
    ...

def _validate_category_name(category: str) -> str:
    ...

def _validate_bundle_rel_path(rel_path: str) -> str:
    ...

class GitHubAuth:
    def __init__(self: Self) -> None: ...
    def get_headers(self: Self) -> Dict[str, str]: ...
    def is_authenticated(self: Self) -> bool: ...
    def auth_method(self: Self) -> str: ...
    def _resolve_token(self: Self) -> Optional[str]: ...
    def _try_gh_cli(self: Self) -> Optional[str]: ...
    def _try_github_app(self: Self) -> Optional[str]: ...

class SkillSource(ABC):
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def source_id(self: Self) -> str: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...

class GitHubSource(SkillSource):
    def __init__(self: Self, auth: GitHubAuth, extra_taps: Optional[List[Dict]]) -> None: ...
    def source_id(self: Self) -> str: ...
    def is_rate_limited(self: Self) -> bool: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def _list_skills_in_repo(self: Self, repo: str, path: str) -> List[SkillMeta]: ...
    def _get_repo_tree(self: Self, repo: str) -> Optional[Tuple[str, List[dict]]]: ...
    def _check_rate_limit_response(self: Self, resp: 'httpx.Response') -> None: ...
    def _download_directory(self: Self, repo: str, path: str) -> Dict[str, str]: ...
    def _download_directory_via_tree(self: Self, repo: str, path: str) -> Optional[Dict[str, str]]: ...
    def _download_directory_recursive(self: Self, repo: str, path: str) -> Dict[str, str]: ...
    def _find_skill_in_repo_tree(self: Self, repo: str, skill_name: str) -> Optional[str]: ...
    def _fetch_file_content(self: Self, repo: str, path: str) -> Optional[str]: ...
    def _read_cache(self: Self, key: str) -> Optional[list]: ...
    def _write_cache(self: Self, key: str, data: list) -> None: ...
    @staticmethod
    def _meta_to_dict(meta: SkillMeta) -> dict: ...
    @staticmethod
    def _parse_frontmatter_quick(content: str) -> dict: ...

class WellKnownSkillSource(SkillSource):
    def source_id(self: Self) -> str: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def _query_to_index_url(self: Self, query: str) -> Optional[str]: ...
    def _parse_identifier(self: Self, identifier: str) -> Optional[dict]: ...
    def _parse_index(self: Self, index_url: str) -> Optional[dict]: ...
    def _index_entry(self: Self, index_url: str, skill_name: str) -> Optional[dict]: ...
    @staticmethod
    def _fetch_text(url: str) -> Optional[str]: ...
    @staticmethod
    def _wrap_identifier(base_url: str, skill_name: str) -> str: ...

class SkillsShSource(SkillSource):
    def __init__(self: Self, auth: GitHubAuth) -> None: ...
    def source_id(self: Self) -> str: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def _featured_skills(self: Self, limit: int) -> List[SkillMeta]: ...
    def _meta_from_search_item(self: Self, item: dict) -> Optional[SkillMeta]: ...
    def _fetch_detail_page(self: Self, identifier: str) -> Optional[dict]: ...
    def _parse_detail_page(self: Self, identifier: str, html: str) -> Optional[dict]: ...
    def _discover_identifier(self: Self, identifier: str, detail: Optional[dict]) -> Optional[str]: ...
    def _resolve_github_meta(self: Self, identifier: str, detail: Optional[dict]) -> Optional[SkillMeta]: ...
    def _finalize_inspect_meta(self: Self, meta: SkillMeta, canonical: str, detail: Optional[dict]) -> SkillMeta: ...
    @classmethod
    def _matches_skill_tokens(cls, meta: SkillMeta, skill_tokens: List[str]) -> bool: ...
    @staticmethod
    def _token_variants(value: Optional[str]) -> set[str]: ...
    @staticmethod
    def _extract_repo_slug(repo_value: str) -> Optional[str]: ...
    @staticmethod
    def _extract_first_match(pattern: re.Pattern, text: str) -> Optional[str]: ...
    def _detail_to_metadata(self: Self, canonical: str, detail: Optional[dict]) -> Dict[str, Any]: ...
    @staticmethod
    def _extract_weekly_installs(html: str) -> Optional[str]: ...
    @staticmethod
    def _extract_security_audits(html: str, identifier: str) -> Dict[str, str]: ...
    @staticmethod
    def _strip_html(value: str) -> str: ...
    @staticmethod
    def _normalize_identifier(identifier: str) -> str: ...
    @staticmethod
    def _candidate_identifiers(identifier: str) -> List[str]: ...
    @staticmethod
    def _wrap_identifier(identifier: str) -> str: ...

class ClawHubSource(SkillSource):
    def source_id(self: Self) -> str: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    @staticmethod
    def _normalize_tags(tags: Any) -> List[str]: ...
    @staticmethod
    def _coerce_skill_payload(data: Any) -> Optional[Dict[str, Any]]: ...
    @staticmethod
    def _query_terms(query: str) -> List[str]: ...
    @classmethod
    def _search_score(cls, query: str, meta: SkillMeta) -> int: ...
    @staticmethod
    def _dedupe_results(results: List[SkillMeta]) -> List[SkillMeta]: ...
    def _exact_slug_meta(self: Self, query: str) -> Optional[SkillMeta]: ...
    def _finalize_search_results(self: Self, query: str, results: List[SkillMeta], limit: int) -> List[SkillMeta]: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def _search_catalog(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def _load_catalog_index(self: Self) -> List[SkillMeta]: ...
    def _get_json(self: Self, url: str, timeout: int) -> Optional[Any]: ...
    def _resolve_latest_version(self: Self, slug: str, skill_data: Dict[str, Any]) -> Optional[str]: ...
    def _extract_files(self: Self, version_data: Dict[str, Any]) -> Dict[str, str]: ...
    def _download_zip(self: Self, slug: str, version: str) -> Dict[str, str]: ...
    def _fetch_text(self: Self, url: str) -> Optional[str]: ...

class ClaudeMarketplaceSource(SkillSource):
    def __init__(self: Self, auth: GitHubAuth) -> None: ...
    def source_id(self: Self) -> str: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def _fetch_marketplace_index(self: Self, repo: str) -> List[dict]: ...

class LobeHubSource(SkillSource):
    def source_id(self: Self) -> str: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def _fetch_index(self: Self) -> Optional[Any]: ...
    def _fetch_agent(self: Self, agent_id: str) -> Optional[dict]: ...
    @staticmethod
    def _convert_to_skill_md(agent_data: dict) -> str: ...

class OptionalSkillSource(SkillSource):
    def __init__(self: Self) -> None: ...
    def source_id(self: Self) -> str: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def _find_skill_dir(self: Self, name: str) -> Optional[Path]: ...
    def _scan_all(self: Self) -> List[SkillMeta]: ...
    @staticmethod
    def _parse_frontmatter(content: str) -> dict: ...

def _read_index_cache(key: str) -> Optional[Any]:
    ...

def _write_index_cache(key: str, data: Any) -> None:
    ...

def _skill_meta_to_dict(meta: SkillMeta) -> dict:
    ...

class HubLockFile:
    def __init__(self: Self, path: Path) -> None: ...
    def load(self: Self) -> dict: ...
    def save(self: Self, data: dict) -> None: ...
    def record_install(self: Self, name: str, source: str, identifier: str, trust_level: str, scan_verdict: str, skill_hash: str, install_path: str, files: List[str], metadata: Optional[Dict[str, Any]]) -> None: ...
    def record_uninstall(self: Self, name: str) -> None: ...
    def get_installed(self: Self, name: str) -> Optional[dict]: ...
    def list_installed(self: Self) -> List[dict]: ...

class TapsManager:
    def __init__(self: Self, path: Path) -> None: ...
    def load(self: Self) -> List[dict]: ...
    def save(self: Self, taps: List[dict]) -> None: ...
    def add(self: Self, repo: str, path: str) -> bool: ...
    def remove(self: Self, repo: str) -> bool: ...
    def list_taps(self: Self) -> List[dict]: ...

def append_audit_log(action: str, skill_name: str, source: str, trust_level: str, verdict: str, extra: str) -> None:
    ...

def ensure_hub_dirs() -> None:
    ...

def quarantine_bundle(bundle: SkillBundle) -> Path:
    ...

def install_from_quarantine(quarantine_path: Path, skill_name: str, category: str, bundle: SkillBundle, scan_result: ScanResult) -> Path:
    ...

def uninstall_skill(skill_name: str) -> Tuple[bool, str]:
    ...

def bundle_content_hash(bundle: SkillBundle) -> str:
    ...

def _source_matches(source: SkillSource, source_name: str) -> bool:
    ...

def check_for_skill_updates(name: Optional[str]) -> List[dict]:
    ...

HERMES_INDEX_URL = 'https://hermes-agent.nousresearch.com/docs/api/skills-index.json'
HERMES_INDEX_CACHE_FILE = INDEX_CACHE_DIR / 'hermes-index.json'
HERMES_INDEX_TTL = 6 * 3600
def _load_hermes_index() -> Optional[dict]:
    ...

def _load_stale_index_cache() -> Optional[dict]:
    ...

class HermesIndexSource(SkillSource):
    def __init__(self: Self, auth: GitHubAuth) -> None: ...
    def _ensure_loaded(self: Self) -> dict: ...
    def _get_github(self: Self) -> GitHubSource: ...
    def source_id(self: Self) -> str: ...
    def is_available(self: Self) -> bool: ...
    def trust_level_for(self: Self, identifier: str) -> str: ...
    def search(self: Self, query: str, limit: int) -> List[SkillMeta]: ...
    def fetch(self: Self, identifier: str) -> Optional[SkillBundle]: ...
    def inspect(self: Self, identifier: str) -> Optional[SkillMeta]: ...
    def _find_entry(self: Self, identifier: str, index: dict) -> Optional[dict]: ...
    @staticmethod
    def _to_meta(entry: dict) -> SkillMeta: ...

def create_source_router(auth: Optional[GitHubAuth]) -> List[SkillSource]:
    ...

def _search_one_source(src: SkillSource, query: str, limit: int) -> Tuple[str, List[SkillMeta]]:
    ...

def parallel_search_sources(sources: List[SkillSource], query: str, per_source_limits: Optional[Dict[str, int]], source_filter: str, overall_timeout: float, on_source_done: Optional[Any]) -> Tuple[List[SkillMeta], Dict[str, int], List[str]]:
    ...

def unified_search(query: str, sources: List[SkillSource], source_filter: str, limit: int) -> List[SkillMeta]:
    ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import hashlib
import json
import logging
import os
import re
import shutil
import subprocess
import time
import abc
import dataclasses
import datetime
import pathlib
import hermes_constants
import typing
import urllib
import urllib.parse
import httpx
import yaml
import tools
import tools.skills_guard
import jwt
import io
import zipfile
import concurrent
import concurrent.futures
import concurrent.futures.ThreadPoolExecutor
import concurrent.futures.as_completed