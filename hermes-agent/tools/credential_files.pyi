# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from __future__ import annotations
from contextvars import ContextVar
from pathlib import Path
from typing import Dict, List
import logging
import os

_registered_files_var: ContextVar[Dict[str, str]] = ContextVar('_registered_files')
def _get_registered() -> Dict[str, str]:
    ...

_config_files: List[Dict[str, str]] | None = None
def _resolve_hermes_home() -> Path:
    ...

def register_credential_file(relative_path: str, container_base: str) -> bool:
    ...

def register_credential_files(entries: list, container_base: str) -> List[str]:
    ...

def _load_config_files() -> List[Dict[str, str]]:
    ...

def get_credential_file_mounts() -> List[Dict[str, str]]:
    ...

def get_skills_directory_mount(container_base: str) -> list[Dict[str, str]]:
    ...

_safe_skills_tempdir: Path | None = None
def _safe_skills_path(skills_dir: Path) -> str:
    ...

def iter_skills_files(container_base: str) -> List[Dict[str, str]]:
    ...

_CACHE_DIRS: list[tuple[str, str]] = [('cache/documents', 'document_cache'), ('cache/images', 'image_cache'), ('cache/audio', 'audio_cache'), ('cache/screenshots', 'browser_screenshots')]
def get_cache_directory_mounts(container_base: str) -> List[Dict[str, str]]:
    ...

def iter_cache_files(container_base: str) -> List[Dict[str, str]]:
    ...

def clear_credential_files() -> None:
    ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import __future__
import logging
import os
import contextvars
import pathlib
import typing
import hermes_constants
import posixpath
import tools
import tools.path_security
import hermes_cli
import hermes_cli.config
import agent
import agent.skill_utils
import atexit
import shutil
import tempfile