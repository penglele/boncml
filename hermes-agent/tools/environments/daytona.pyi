# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from pathlib import Path
from tools.environments.base import BaseEnvironment, _ThreadedProcessHandle
from tools.environments.file_sync import FileSyncManager, iter_sync_files, quoted_mkdir_command, quoted_rm_command, unique_parent_dirs
from typing import Any
from typing_extensions import Self
import logging
import math
import shlex
import threading

class DaytonaEnvironment(BaseEnvironment):
    def __init__(self: Self, image: str, cwd: str, timeout: int, cpu: int, memory: int, disk: int, persistent_filesystem: bool, task_id: str) -> None: ...
    def _daytona_upload(self: Self, host_path: str, remote_path: str) -> None: ...
    def _daytona_bulk_upload(self: Self, files: list[tuple[str, str]]) -> None: ...
    def _daytona_delete(self: Self, remote_paths: list[str]) -> None: ...
    def _ensure_sandbox_ready(self: Self) -> None: ...
    def _before_execute(self: Self) -> None: ...
    def _run_bash(self: Self, cmd_string: str) -> Any: ...
    def cleanup(self: Self) -> Any: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import logging
import math
import shlex
import threading
import pathlib
import tools
import tools.environments
import tools.environments.base
import tools.environments.file_sync
import daytona.common
import daytona.common.filesystem