# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from __future__ import annotations
from abc import abstractmethod
from dataclasses import dataclass
from tools.environments.base import BaseEnvironment
from tools.interrupt import is_interrupted
from typing import Any
from typing_extensions import Self
from dataclasses import dataclass
import shlex
import time
import uuid

@dataclass
class PreparedModalExec:
@dataclass
class ModalExecStart:
def wrap_modal_stdin_heredoc(command: str, stdin_data: str) -> str:
    ...

def wrap_modal_sudo_pipe(command: str, sudo_stdin: str) -> str:
    ...

@dataclass
class BaseModalExecutionEnvironment(BaseEnvironment):
    def execute(self: Self, command: str, cwd: str) -> dict: ...
    def _before_execute(self: Self) -> None: ...
    def _prepare_modal_exec(self: Self, command: str) -> PreparedModalExec: ...
    def _result(self: Self, output: str, returncode: int) -> dict: ...
    def _error_result(self: Self, output: str) -> dict: ...
    def _timeout_result_for_modal(self: Self, timeout: int) -> dict: ...
    def _start_modal_exec(self: Self, prepared: PreparedModalExec) -> ModalExecStart: ...
    def _poll_modal_exec(self: Self, handle: Any) -> dict | None: ...
    def _cancel_modal_exec(self: Self, handle: Any) -> None: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import __future__
import shlex
import time
import uuid
import abc
import dataclasses
import typing
import tools
import tools.environments
import tools.environments.base
import tools.interrupt