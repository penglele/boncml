# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from tools.debug_helpers import DebugSession
from tools.managed_tool_gateway import resolve_managed_tool_gateway
from tools.registry import registry, tool_error
from tools.tool_backend_helpers import managed_nous_tools_enabled
from typing import Any, Dict, Optional, Union
from typing_extensions import Self
from urllib.parse import urlencode
import datetime
import fal_client
import json
import logging
import os
import threading
import uuid

DEFAULT_MODEL = 'fal-ai/flux-2-pro'
DEFAULT_ASPECT_RATIO = 'landscape'
DEFAULT_NUM_INFERENCE_STEPS = 50
DEFAULT_GUIDANCE_SCALE = 4.5
DEFAULT_NUM_IMAGES = 1
DEFAULT_OUTPUT_FORMAT = 'png'
ENABLE_SAFETY_CHECKER = False
SAFETY_TOLERANCE = '5'
ASPECT_RATIO_MAP = {'landscape': 'landscape_16_9', 'square': 'square_hd', 'portrait': 'portrait_16_9'}
UPSCALER_MODEL = 'fal-ai/clarity-upscaler'
UPSCALER_FACTOR = 2
UPSCALER_SAFETY_CHECKER = False
UPSCALER_DEFAULT_PROMPT = 'masterpiece, best quality, highres'
UPSCALER_NEGATIVE_PROMPT = '(worst quality, low quality, normal quality:2)'
UPSCALER_CREATIVITY = 0.35
UPSCALER_RESEMBLANCE = 0.6
UPSCALER_GUIDANCE_SCALE = 4
UPSCALER_NUM_INFERENCE_STEPS = 18
VALID_IMAGE_SIZES = ['square_hd', 'square', 'portrait_4_3', 'portrait_16_9', 'landscape_4_3', 'landscape_16_9']
VALID_OUTPUT_FORMATS = ['jpeg', 'png']
VALID_ACCELERATION_MODES = ['none', 'regular', 'high']
_managed_fal_client = None
_managed_fal_client_config = None
def _resolve_managed_fal_gateway() -> Any:
    ...

def _normalize_fal_queue_url_format(queue_run_origin: str) -> str:
    ...

class _ManagedFalSyncClient:
    def __init__(self: Self) -> None: ...
    def submit(self: Self, application: str, arguments: Dict[str, Any]) -> Any: ...

def _get_managed_fal_client(managed_gateway: Any) -> Any:
    ...

def _submit_fal_request(model: str, arguments: Dict[str, Any]) -> Any:
    ...

def _validate_parameters(image_size: Union[str, Dict[str, int]], num_inference_steps: int, guidance_scale: float, num_images: int, output_format: str, acceleration: str) -> Dict[str, Any]:
    ...

def _upscale_image(image_url: str, original_prompt: str) -> Dict[str, Any]:
    ...

def image_generate_tool(prompt: str, aspect_ratio: str, num_inference_steps: int, guidance_scale: float, num_images: int, output_format: str, seed: Optional[int]) -> str:
    ...

def check_fal_api_key() -> bool:
    ...

def check_image_generation_requirements() -> bool:
    ...

def get_debug_session_info() -> Dict[str, Any]:
    ...

IMAGE_GENERATE_SCHEMA = {'name': 'image_generate', 'description': 'Generate high-quality images from text prompts using FLUX 2 Pro model with automatic 2x upscaling. Creates detailed, artistic images that are automatically upscaled for hi-rez results. Returns a single upscaled image URL. Display it using markdown: ![description](URL)', 'parameters': {'type': 'object', 'properties': {'prompt': {'type': 'string', 'description': 'The text prompt describing the desired image. Be detailed and descriptive.'}, 'aspect_ratio': {'type': 'string', 'enum': ['landscape', 'square', 'portrait'], 'description': "The aspect ratio of the generated image. 'landscape' is 16:9 wide, 'portrait' is 16:9 tall, 'square' is 1:1.", 'default': 'landscape'}}, 'required': ['prompt']}}
def _handle_image_generate(args: Any) -> Any:
    ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import json
import logging
import os
import datetime
import threading
import uuid
import typing
import urllib
import urllib.parse
import fal_client
import tools
import tools.debug_helpers
import tools.managed_tool_gateway
import tools.tool_backend_helpers
import tools.registry