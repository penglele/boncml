# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from tools.budget_config import BudgetConfig, DEFAULT_BUDGET, DEFAULT_PREVIEW_SIZE_CHARS
from typing import Any
import logging
import os
import shlex
import uuid

PERSISTED_OUTPUT_TAG = '<persisted-output>'
PERSISTED_OUTPUT_CLOSING_TAG = '</persisted-output>'
STORAGE_DIR = '/tmp/hermes-results'
HEREDOC_MARKER = 'HERMES_PERSIST_EOF'
_BUDGET_TOOL_NAME = '__budget_enforcement__'
def _resolve_storage_dir(env: Any) -> str:
    ...

def generate_preview(content: str, max_chars: int) -> tuple[str, bool]:
    ...

def _heredoc_marker(content: str) -> str:
    ...

def _write_to_sandbox(content: str, remote_path: str, env: Any) -> bool:
    ...

def _build_persisted_message(preview: str, has_more: bool, original_size: int, file_path: str) -> str:
    ...

def maybe_persist_tool_result(content: str, tool_name: str, tool_use_id: str, env: Any, config: BudgetConfig, threshold: int | float | None) -> str:
    ...

def enforce_turn_budget(tool_messages: list[dict], env: Any, config: BudgetConfig) -> list[dict]:
    ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import logging
import os
import shlex
import uuid
import tools
import tools.budget_config