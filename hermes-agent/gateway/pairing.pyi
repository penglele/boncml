# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from hermes_constants import get_hermes_dir
from pathlib import Path
from typing import Any, Optional
from typing_extensions import Self
import json
import os
import secrets
import tempfile
import threading
import time

ALPHABET = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'
CODE_LENGTH = 8
CODE_TTL_SECONDS = 3600
RATE_LIMIT_SECONDS = 600
LOCKOUT_SECONDS = 3600
MAX_PENDING_PER_PLATFORM = 3
MAX_FAILED_ATTEMPTS = 5
def _secure_write(path: Path, data: str) -> None:
    ...

class PairingStore:
    def __init__(self: Self) -> None: ...
    def _pending_path(self: Self, platform: str) -> Path: ...
    def _approved_path(self: Self, platform: str) -> Path: ...
    def _rate_limit_path(self: Self) -> Path: ...
    def _load_json(self: Self, path: Path) -> dict: ...
    def _save_json(self: Self, path: Path, data: dict) -> None: ...
    def is_approved(self: Self, platform: str, user_id: str) -> bool: ...
    def list_approved(self: Self, platform: str) -> list: ...
    def _approve_user(self: Self, platform: str, user_id: str, user_name: str) -> None: ...
    def revoke(self: Self, platform: str, user_id: str) -> bool: ...
    def generate_code(self: Self, platform: str, user_id: str, user_name: str) -> Optional[str]: ...
    def approve_code(self: Self, platform: str, code: str) -> Optional[dict]: ...
    def list_pending(self: Self, platform: str) -> list: ...
    def clear_pending(self: Self, platform: str) -> int: ...
    def _is_rate_limited(self: Self, platform: str, user_id: str) -> bool: ...
    def _record_rate_limit(self: Self, platform: str, user_id: str) -> None: ...
    def _is_locked_out(self: Self, platform: str) -> bool: ...
    def _record_failed_attempt(self: Self, platform: str) -> None: ...
    def _cleanup_expired(self: Self, platform: str) -> None: ...
    def _all_platforms(self: Self, suffix: str) -> list: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import json
import os
import secrets
import tempfile
import threading
import time
import pathlib
import typing
import hermes_constants