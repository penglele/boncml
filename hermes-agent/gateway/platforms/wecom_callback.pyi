# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from __future__ import annotations
from aiohttp import web
from gateway.config import Platform, PlatformConfig
from gateway.platforms.base import BasePlatformAdapter, MessageEvent, MessageType, SendResult
from gateway.platforms.wecom_crypto import WXBizMsgCrypt, WeComCryptoError
from typing import Any, Dict, List, Optional
from typing_extensions import Self
from xml.etree import ElementTree
import asyncio
import httpx
import logging
import socket
import time

AIOHTTP_AVAILABLE = True
web = None
AIOHTTP_AVAILABLE = False
HTTPX_AVAILABLE = True
httpx = None
HTTPX_AVAILABLE = False
DEFAULT_HOST = '0.0.0.0'
DEFAULT_PORT = 8645
DEFAULT_PATH = '/wecom/callback'
ACCESS_TOKEN_TTL_SECONDS = 7200
MESSAGE_DEDUP_TTL_SECONDS = 300
def check_wecom_callback_requirements() -> bool:
    ...

class WecomCallbackAdapter(BasePlatformAdapter):
    def __init__(self: Self, config: PlatformConfig) -> None: ...
    @staticmethod
    def _user_app_key(corp_id: str, user_id: str) -> str: ...
    @staticmethod
    def _normalize_apps(extra: Dict[str, Any]) -> List[Dict[str, Any]]: ...
    def _resolve_app_for_chat(self: Self, chat_id: str) -> Dict[str, Any]: ...
    def _decrypt_request(self: Self, app: Dict[str, Any], body: str, msg_signature: str, timestamp: str, nonce: str) -> str: ...
    def _build_event(self: Self, app: Dict[str, Any], xml_text: str) -> Optional[MessageEvent]: ...
    def _crypt_for_app(self: Self, app: Dict[str, Any]) -> WXBizMsgCrypt: ...
    def _get_app_by_name(self: Self, name: Optional[str]) -> Optional[Dict[str, Any]]: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import __future__
import asyncio
import logging
import socket
import time
import typing
import xml
import xml.etree
import xml.etree.ElementTree
import aiohttp
import httpx
import gateway
import gateway.config
import gateway.platforms
import gateway.platforms.base
import gateway.platforms.wecom_crypto