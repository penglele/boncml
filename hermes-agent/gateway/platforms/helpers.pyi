# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from gateway.platforms.base import BasePlatformAdapter, MessageEvent
from pathlib import Path
from typing import Any, Dict, Optional, TYPE_CHECKING
from typing_extensions import Self
import asyncio
import json
import logging
import re
import time

class MessageDeduplicator:
    def __init__(self: Self, max_size: int, ttl_seconds: float) -> None: ...
    def is_duplicate(self: Self, msg_id: str) -> bool: ...
    def clear(self: Self) -> Any: ...

class TextBatchAggregator:
    def __init__(self: Self, handler: Any) -> None: ...
    def is_enabled(self: Self) -> bool: ...
    def enqueue(self: Self, event: 'MessageEvent', key: str) -> None: ...
    def cancel_all(self: Self) -> None: ...

def strip_markdown(text: str) -> str:
    ...

class ThreadParticipationTracker:
    def __init__(self: Self, platform_name: str, max_tracked: int) -> None: ...
    def _state_path(self: Self) -> Path: ...
    def _load(self: Self) -> set: ...
    def _save(self: Self) -> None: ...
    def mark(self: Self, thread_id: str) -> None: ...
    def __contains__(self: Self, thread_id: str) -> bool: ...
    def clear(self: Self) -> None: ...

def redact_phone(phone: str) -> str:
    ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import asyncio
import json
import logging
import re
import time
import pathlib
import typing
import hermes_constants