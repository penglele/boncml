# This file was generated by Nuitka

# Stubs included by default
from __future__ import annotations
from __future__ import annotations
from dataclasses import dataclass
from typing import Any, Optional
from typing_extensions import Self
from dataclasses import dataclass
import asyncio
import logging
import queue
import re
import time

@dataclass
class StreamConsumerConfig:
class GatewayStreamConsumer:
    def __init__(self: Self, adapter: Any, chat_id: str, config: Optional[StreamConsumerConfig], metadata: Optional[dict]) -> None: ...
    def already_sent(self: Self) -> bool: ...
    def final_response_sent(self: Self) -> bool: ...
    def on_segment_break(self: Self) -> None: ...
    def on_commentary(self: Self, text: str) -> None: ...
    def _reset_segment_state(self: Self) -> None: ...
    def on_delta(self: Self, text: str) -> None: ...
    def finish(self: Self) -> None: ...
    @staticmethod
    def _clean_for_display(text: str) -> str: ...
    def _visible_prefix(self: Self) -> str: ...
    def _continuation_text(self: Self, final_text: str) -> str: ...
    @staticmethod
    def _split_text_chunks(text: str, limit: int) -> list[str]: ...
    def _is_flood_error(self: Self, result: Any) -> bool: ...


__name__ = ...



# Modules used internally, to allow implicit dependencies to be seen:
import __future__
import asyncio
import logging
import queue
import re
import time
import dataclasses
import typing